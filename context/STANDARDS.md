# STANDARDS.md

> Rules for humans. Descriptive statements. At least five, covering the areas below.
> Restate each one as an imperative instruction in CLAUDE.md.

**Source of truth if these files disagree:** [STANDARDS.md or CLAUDE.md]

## Naming
[e.g. camelCase (likeThis) for variables and functions; kebab-case (like-this) for file names; no abbreviations under four letters.]

## Structure
[e.g. HTML (structure), CSS (appearance), and JS (behavior) in separate files. Structure before style before behavior.]

## Comments
[e.g. Comments explain why, never what.]

## Commits
[e.g. Commit messages (the note you write when you save to GitHub) in present tense, under 60 characters, starting with a verb: "Add save button" rather than "added stuff."]

## Forbidden
[e.g. No inline styles (a style= attribute inside an HTML tag). No innerHTML with anything a user typed (use textContent). No console.log (debugging printouts) left in a submission.]
